services:
  overtime-tracker:
    build: .
    container_name: overtime-tracker
    restart: unless-stopped
    ports:
      - "127.0.0.1:3090:80"
    volumes:
      - /root/overtime-tracker/data:/data
